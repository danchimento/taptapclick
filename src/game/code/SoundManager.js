export default class SoundManager {

    playSound(soundName) {
        // let sound = new Sound(soundName + '.mp3', Sound.MAIN_BUNDLE, (error) => {
        //     sound.play();
        // });  
    }
}